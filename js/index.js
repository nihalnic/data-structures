// EVERYTHING WILL BE FINE :)

import express from "express";

const app = express();

app.listen();
{
}
const mindset = "Everything will be fine.";
let limit = 0;

while (limit < unlimited) {
  console.log(mindset);
  limit++;
}
